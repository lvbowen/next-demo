import { useRouter } from 'next/router';

const id = (props) => {
     const router = useRouter();
     console.log('post detail router', router)

    return (
        <div>idiiddidi </div>
    )
}

export default id;
